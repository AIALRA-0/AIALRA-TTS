name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  test:
    name: Tests and release gate
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install package
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest

      - name: Python tests
        shell: pwsh
        run: python -m pytest -q

      - name: Compile Python package
        shell: pwsh
        run: python -m compileall .\src\ecse_localizer

      - name: Check WebUI JavaScript
        shell: pwsh
        run: node --check .\src\ecse_localizer\static\app.js

      - name: Check PowerShell scripts
        shell: pwsh
        run: .\tools\check_powershell_syntax.ps1

      - name: Secret scan
        shell: pwsh
        run: .\tools\secret_scan.ps1

      - name: Translation sample gate
        shell: pwsh
        run: python -m ecse_localizer --config ".\config.example.yaml" translation-sample --output ".\runs\translation_quality_sample"

      - name: Remote worker queue smoke
        shell: pwsh
        run: python -m ecse_localizer --config ".\config.example.yaml" remote-smoke --output ".\runs\remote_smoke"

      - name: Worker health local gate
        shell: pwsh
        run: python -m ecse_localizer --config ".\config.example.yaml" worker-health --skip-remote

      - name: Platform aggregate gate
        shell: pwsh
        run: python -m ecse_localizer --config ".\config.example.yaml" platform-check --output ".\runs\platform_check"

      - name: Release metadata gate
        shell: pwsh
        run: python -m ecse_localizer --config ".\config.example.yaml" release-check
